<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Blog</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
    <main class="max-w-2xl mx-auto mt-10 bg-white rounded-lg shadow-lg p-8">
        <h1 class="text-2xl font-bold mb-6">Edit Blog Post</h1>
        <form action="/edit/<%= blog.id %>" method="POST" enctype="multipart/form-data">
            <div class="mb-4">
                <label class="block mb-1 font-medium">Title</label>
                <input type="text" name="title" value="<%= blog.title %>" class="w-full border px-3 py-2 rounded" required>
            </div>
            <div class="mb-4">
                <label class="block mb-1 font-medium">Subtitle</label>
                <input type="text" name="subtitle" value="<%= blog.subtitle %>" class="w-full border px-3 py-2 rounded" required>
            </div>
            <div class="mb-4">
                <label class="block mb-1 font-medium">Description</label>
                <textarea name="description" class="w-full border px-3 py-2 rounded" required><%= blog.description %></textarea>
            </div>
            <div class="mb-4">
                <label class="block mb-1 font-medium">Image</label>
                <input type="file" name="image" class="w-full">
                <% if (blog.image) { %>
                    <img src="http://localhost:3000/<%= blog.image %>" alt="Current Image" class="mt-2 h-32">
                <% } %>
            </div>
            <button type="submit" class="bg-indigo-600 text-white px-4 py-2 rounded hover:bg-indigo-700">Update</button>
            <a href="/blog/<%= blog.id %>" class="ml-4 text-gray-600 hover:underline">Cancel</a>
        </form>
    </main>
</body>
</html>